unit SDenums;

{$mode ObjFPC}{$H+}

interface

uses
  Classes, SysUtils;

type
  TTypeMessage = (tmInfo, tmWarning, tmError, tmQuestion);
  TCanceledOrConfirmed = (Canceled, Confirmed);

implementation

end.
